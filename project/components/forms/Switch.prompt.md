Toggle for boolean settings — "Track as streak", "Use global default".

```jsx
<Switch checked={on} onChange={setOn} color="var(--habit)" />
```

Pass `color` to tint the on-state track (e.g. habit violet for streak mode).
