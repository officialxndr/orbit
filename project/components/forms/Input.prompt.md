Labeled single-line text field with optional leading icon.

```jsx
<Input label="Title" value={v} onChange={e=>setV(e.target.value)} icon="pen-line" />
```

Pass `error` for the invalid state. For list search use `SearchBar` instead.
