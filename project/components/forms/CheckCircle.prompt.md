Circular completion toggle — springs in a checkmark when done. Color it with the entity-type accent.

```jsx
<CheckCircle checked={done} onChange={setDone} color="var(--task)" />
```

`EntityRow` renders one automatically; use this standalone in custom rows.
