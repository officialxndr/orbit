Rounded search field that sits atop any list (Today, Items, link-picker).

```jsx
<SearchBar value={q} onChange={e=>setQ(e.target.value)} onClear={()=>setQ('')} placeholder="Search people & items" />
```
