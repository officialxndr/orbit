Icon-only control for headers, toolbars and row affordances.

```jsx
<IconButton icon="bell" variant="soft" ariaLabel="Notifications" />
<IconButton icon="plus" variant="solid" round />
```

Variants: `solid | soft | ghost | outline`. Always pass `ariaLabel`. Use `round` for circular.
