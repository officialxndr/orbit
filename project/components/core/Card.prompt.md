Base surface container. Add `accent` for the entity-type left border.

```jsx
<Card accent="var(--person)" interactive onClick={open}>
  …content…
</Card>
```

For full reminder/list rows prefer `EntityRow`, which builds on this pattern.
