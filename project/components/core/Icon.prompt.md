Renders a Lucide glyph. Requires the Lucide UMD script on the page (`<script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js">`).

```jsx
<Icon name="user-round" size={20} color="var(--person)" />
```

Names are Lucide kebab-case. Used internally by Button, TabBar, EntityIcon, etc.
