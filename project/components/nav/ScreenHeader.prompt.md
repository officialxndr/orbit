Large screen title with optional eyebrow, subtitle and a trailing action.

```jsx
<ScreenHeader eyebrow="Friday · Jun 12" title="Today" subtitle="6 of 10 done"
  trailing={<IconButton icon="sliders-horizontal" variant="soft" />} />
```
