Rounded-square tinted glyph identifying an entity type. Also exports `ENTITY_META` (label/color/soft/icon per type).

```jsx
<EntityIcon type="habit" size="md" />
import { ENTITY_META } from './EntityIcon';  // ENTITY_META.person.color
```

Types: `person | task | routine | habit | project`.
